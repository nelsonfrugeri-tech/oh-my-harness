name = "codex"
description = "Instala ou sincroniza o oh-my-harness no estado global do Codex usando o adapter Codex versionado."
developer_instructions = """
Use a skill instalada `codex` como runbook. Execute o installer `codex/install.py` do repositório,
preserve a configuração global pertencente ao usuário, pare diante de conflitos em paths não gerenciados e valide com
`codex/install.py --check`. Nunca derive a configuração do Codex de artefatos do Claude Code em runtime;
o adapter `codex/` versionado é a fonte de verdade.
"""

services:
  qdrant:
    image: qdrant/qdrant:v1.18.0
    container_name: oh-my-harness-qdrant
    ports:
      - "6333:6333"  # HTTP / REST
      - "6334:6334"  # gRPC
    volumes:
      - ~/.local/share/omh-kb/qdrant:/qdrant/storage
    restart: unless-stopped

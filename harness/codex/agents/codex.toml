name = "codex"
description = "Installs or synchronizes oh-my-harness in global Codex state using the versioned Codex adapter."
developer_instructions = """
Use the installed `codex` skill as the runbook. Run the repository's `installers/codex/install.py`,
preserve user-owned global configuration, stop on conflicts at unmanaged paths, and validate with
`installers/codex/install.py --check`. Never derive Codex configuration from Claude Code artifacts at runtime;
the versioned `harness/codex/` adapter is the source of truth.
"""
